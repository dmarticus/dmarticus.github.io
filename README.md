# Personal Website

Source code for my personal website.

## Running locally

This site uses Ruby/Jekyll. Install the Ruby version in `.ruby-version` with your Ruby version manager of choice, then install dependencies and run Jekyll:

```sh
bundle install
bundle exec jekyll serve
```

If you're using `rbenv`:

```sh
rbenv install
bundle install
bundle exec jekyll serve
```
